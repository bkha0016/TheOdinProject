// no error
let message = "hello";
message = 123456; //changing the datatype from string into number

let n = 123; //integer
n = 12.345; //float
/* 
alert(1/0); alert(Infinity);
alert("not a number"/2); //NAN, such division is erroneous
alert(NaN + 1); alert(3 * NaN);
*/

// the "n" at the end means its a BigInt
const bigInt = 12345678793129837819739127391273981321n;

//STRING
let str = "Hello";
let str2 = 'Singe quotes are ok too';
let phrase = `can embed another ${str}`; //use backtis to imbed a var in a string

let name = "John";

console.log(`Hello, ${name}!`);
console.log(`The results is ${1 + 2}`);

//BOOLEAN (logical type)
let nameFieldChecked = true; //yes, name field is checked
let ageFieldChecked = false; //no, age field is not checked

let isGreater = 4 > 1;

//NULL (does not belong to any of the types described above)
let age = null;

//UNDEFINED (Type of its own)
//OBJECTS(more complex data structure) & SYMBOLS(for unique identifiers)

//TYPEOF operator


console.log("zpple" > "pineapple");



